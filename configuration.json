[
    {
        "key": "ClientId",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "ClientId",
            "description": "The ClientId to connect to the API",
            "required": true
        }
    },
    {
        "key": "ClientSecret",
        "type": "input",
        "templateOptions": {
            "type": "password",
            "label": "ClientSecret",
            "description": "The ClientSecret to connect to the API",
            "required": true
        }
    },
    {
        "key": "OrganisationId",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "OrganisationId",
            "description": "The OrganisationId to connect to the API",
            "required": true
        }
    },
    {
        "key": "Environment",
        "type": "input",
        "defaultValue": "test",
        "templateOptions": {
            "label": "Environment",
            "description": "The Environment name to connect to the API",
            "required": true
        }
    },
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "placeholder": "https://<ServerAddress>/fierit/api",
            "label": "BaseUrl",
            "description": "The URL to the API.",
            "required": true
        }
    },
    {
        "key": "TokenURL",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "placeholder": "https://<ServerAddress>/fierit/api/token/<Environment>",
            "label": "TokenURL",
            "description": "The URL to get the API Token.",
            "required": true
        }
    },
    {
        "key": "DefaultTeamAssignmentGuid",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "DefaultTeamAssignmentGuid",
            "description": "The DefaultTeamAssignmentGuid. Which will be assigned as a 'dummy' Team for a newly created User account.",
            "required": true
        }
    },
    {
        "key": "UseMappingSelectionAuthorisationGroup",
        "type": "checkbox",
        "defaultValue": false,
        "templateOptions": {
            "label": "Toggle to use Mapping",
            "description": "When toggled the mapping file will be used for the SelectionAuthorisationGroup in a Role Assignment",
            "required": true
        }
    },
    {
        "key": "MappingSelectionAuthGroupFileLocation",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "Mapping SelectionAuthGroupFileLocation",
            "description": "Can be used for mapping between Department + (Function) and the SelectionAuthGroup in a Role Assignment",
            "required": false
        }
    },
    {
        "key": "csvDelimiter",
        "type": "input",
        "defaultValue": ";",
        "templateOptions": {
            "label": "csvDelimiter",
            "description": "The csv Delimiter used in the external Mapping file",
            "required": false
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
            "label": "Toggle debug logging",
            "description": "When toggled, debug logging will be displayed",
            "required": false
        }
    }
]